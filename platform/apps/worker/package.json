{
  "name": "@konkur/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check-types": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx watch src/main.ts",
    "migrate": "node dist/migrate.js",
    "start": "node dist/main.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@konkur/config": "workspace:*",
    "@konkur/observability": "workspace:*",
    "graphile-worker": "^0.16.6",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
